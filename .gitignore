target/
node_modules/
.DS_Store
*.log
.env
.env.*
!.env.example
dist/
.wrangler/
apps/phone/.expo/
apps/mac/build/
apps/mac/DerivedData/

# Read-only vendored prior art with its own git history; kept on disk, out of
# this repo's tree so it is never modified or committed as a broken gitlink.
reference/
